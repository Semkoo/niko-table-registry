---
title: Manual Installation
description: Copy and paste all DataTable components into your project manually.
---

import { Steps } from "@astrojs/starlight/components"
import CliCommandCode from "@/components/markdown/cli-command-code/cli-command-code.astro"
import ManualInstallationCode from "@/components/markdown/manual-installation-code/manual-installation-code.astro"

## Prerequisites

Before copying the components, make sure you have:

1. **A React project** (Next.js, Vite, etc.)
2. **Shadcn UI** set up in your project ([Installation Guide](https://ui.shadcn.com/docs/installation))
3. **TailwindCSS** configured
4. **TypeScript** (recommended)

## Architecture Note

The DataTable components use a two-layer architecture for maximum flexibility:

- **Components** (`data-table-*.tsx` in `/components` folder) - Context-aware wrapper components that use `useDataTable()` hook to automatically get the table from `DataTableRoot` context. These eliminate prop drilling and are the recommended way to use components:
  - `DataTableSearchFilter`, `DataTableFilterMenu`, `DataTableSortMenu`, `DataTablePagination`, etc.
- **Filters** (`table-*.tsx` in `/filters` folder) - Core implementation components that accept a `table` prop directly and use TanStack Table hooks (like `table.getState()`, `table.setGlobalFilter()`, etc.). These can be used standalone:
  - `TableSearchFilter`, `TableFilterMenu`, `TableSortMenu`, etc.

**Why this architecture?**

- Use `DataTable*` components from their direct paths (e.g. `@/components/niko-table/components/data-table-pagination`) when you want context-based, zero-config usage
- Use `Table*` components from filters (e.g. `@/components/niko-table/filters/table-pagination`) when you want to build custom components or manage the table instance yourself
- All filter components use TanStack Table hooks directly, giving you full control

## Installation Steps

<Steps>
  1. **Install the required dependencies**

     <CliCommandCode action="install" command="@tanstack/react-table" />

2.  **Install the required Shadcn UI components**

    The registry handles most Shadcn dependencies automatically, but for manual installation you need:

      <CliCommandCode action="run" command="shadcn@latest add button input dropdown-menu"/>

3.  **Copy the custom table component**

    The DataTable requires a custom `components/ui/table.tsx` with a `TableComponent` export. Copy it from the [manual installation files below](#all-components), or install it via the registry CLI (see the [Installation Guide](/getting-started/installation/)).

4.  **Copy all DataTable components**

    Copy [all the files below](#all-components) into your project, maintaining the directory structure shown.

5.  **Update import paths**

    Update the import paths in the copied files to match your project structure. The components use:
    - `@/components/ui/` for Shadcn UI components
    - `@/components/niko-table/` for DataTable components
    - `@/lib/` for utility functions

    Ensure your project has the `@/` path alias pointing at your `src/` directory (or replace `@/` with your own alias in the copied code). **Use direct file imports only** (no barrel exports): import from the specific file (e.g. `@/components/niko-table/core/data-table-root`, `@/components/niko-table/lib/constants`) so bundlers can tree-shake. This matches the shadcn pattern of one entry point per component.

6.  **Verify installation**

    Create a simple test to verify everything is working:

    ```tsx
    import { DataTableRoot } from "@/components/niko-table/core/data-table-root"
    import { DataTable } from "@/components/niko-table/core/data-table"
    import {
      DataTableHeader,
      DataTableBody,
    } from "@/components/niko-table/core/data-table-structure"

    const columns = [
      { accessorKey: "name", header: "Name" },
      { accessorKey: "email", header: "Email" },
    ]

    const data = [
      { id: "1", name: "John Doe", email: "john@example.com" },
      { id: "2", name: "Jane Smith", email: "jane@example.com" },
    ]

    export function TestTable() {
      return (
        <DataTableRoot data={data} columns={columns}>
          <DataTable>
            <DataTableHeader />
            <DataTableBody />
          </DataTable>
        </DataTableRoot>
      )
    }
    ```

</Steps>

## Project Structure

After copying all files, your project structure should look like this:

```
your-project/
├── src/
│   ├── components/
│   │   ├── ui/
│   │   │   ├── table.tsx          # Updated Shadcn table component
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── dropdown-menu.tsx
│   │   │   └── ...                # Other Shadcn components
│   │   │
│   │   └── niko-table/
│   │       │
│   │       ├── core/                        # Core table components
│   │       │   ├── data-table.tsx                                  # Container + scroll wrapper
│   │       │   ├── data-table-root.tsx                             # Top-level provider + table options
│   │       │   ├── data-table-context.tsx                          # useDataTable() context
│   │       │   ├── data-table-error-boundary.tsx                   # Error boundary wrapper
│   │       │   ├── data-table-structure.tsx                        # Header, Body, EmptyBody, Skeleton, Loading, LoadingMore
│   │       │   ├── data-table-row-dnd-structure.tsx               # Row DnD body
│   │       │   ├── data-table-column-dnd-structure.tsx            # Column DnD header/body
│   │       │   ├── data-table-virtualized-structure.tsx            # VirtualizedHeader, VirtualizedBody, VirtualizedEmptyBody, VirtualizedSkeleton, VirtualizedLoading, VirtualizedLoadingMore
│   │       │   ├── data-table-virtualized-row-dnd-structure.tsx   # Virtualized row DnD body
│   │       │   └── data-table-virtualized-column-dnd-structure.tsx # Virtualized column DnD header/body
│   │       │
│   │       ├── components/                  # Context-aware user-facing components
│   │       │   ├── data-table-search-filter.tsx
│   │       │   ├── data-table-view-menu.tsx
│   │       │   ├── data-table-view-dnd-menu.tsx
│   │       │   ├── data-table-filter-menu.tsx
│   │       │   ├── data-table-sort-menu.tsx
│   │       │   ├── data-table-clear-filter.tsx
│   │       │   ├── data-table-faceted-filter.tsx
│   │       │   ├── data-table-slider-filter.tsx
│   │       │   ├── data-table-date-filter.tsx
│   │       │   ├── data-table-inline-filter.tsx
│   │       │   ├── data-table-pagination.tsx
│   │       │   ├── data-table-export-button.tsx
│   │       │   ├── data-table-row-dnd.tsx
│   │       │   ├── data-table-column-dnd.tsx
│   │       │   ├── data-table-column-resize.tsx                   # Opt-in <DataTableColumnResize />
│   │       │   ├── data-table-column-auto-fit.tsx                 # Opt-in <DataTableColumnAutoFit /> (even-split fill)
│   │       │   ├── data-table-column-actions.tsx
│   │       │   ├── data-table-column-header.tsx
│   │       │   ├── data-table-column-title.tsx
│   │       │   ├── data-table-column-sort.tsx
│   │       │   ├── data-table-column-hide.tsx
│   │       │   ├── data-table-column-pin.tsx
│   │       │   ├── data-table-column-filter.tsx
│   │       │   ├── data-table-column-filter-trigger.tsx
│   │       │   ├── data-table-column-faceted-filter.tsx
│   │       │   ├── data-table-column-date-filter-options.tsx
│   │       │   ├── data-table-column-slider-filter-options.tsx
│   │       │   ├── data-table-aside.tsx
│   │       │   ├── data-table-selection-bar.tsx
│   │       │   ├── data-table-toolbar-section.tsx
│   │       │   ├── data-table-row-context-menu.tsx                 # Per-row right-click menu primitive (+ open-row highlight)
│   │       │   ├── data-table-row-context-menu-slot.tsx            # Declarative <DataTableRowContextMenuSlot> for a body
│   │       │   ├── data-table-row-menu.tsx                         # Polymorphic RowMenuItem/Sub/… + DataTableRowMenuScope + useDataTableRow
│   │       │   └── data-table-empty-state.tsx
│   │       │
│   │       ├── grid/                        # Data Grid (optional — editable spreadsheet layer)
│   │       │   ├── core/                      # DataGrid, context, features, columns context
│   │       │   ├── cells/                     # Text / number / select / date / checkbox editors
│   │       │   ├── components/                # Clipboard, fill, move, toolbar, status bar, …
│   │       │   ├── hooks/                     # useDataGrid, useGridColumns, useGridChanges, …
│   │       │   ├── config/                    # Keyboard shortcuts map
│   │       │   └── types/                     # CellState, GridRow, …
│   │       │
│   │       ├── filters/                     # Core filter implementations (accept `table` prop directly)
│   │       │   ├── table-search-filter.tsx
│   │       │   ├── table-faceted-filter.tsx
│   │       │   ├── table-slider-filter.tsx
│   │       │   ├── table-range-filter.tsx
│   │       │   ├── table-date-filter.tsx
│   │       │   ├── table-inline-filter.tsx
│   │       │   ├── table-filter-menu.tsx
│   │       │   ├── table-clear-filter.tsx
│   │       │   ├── table-view-menu.tsx
│   │       │   ├── table-view-dnd-menu.tsx
│   │       │   ├── table-sort-menu.tsx
│   │       │   ├── table-pagination.tsx
│   │       │   ├── table-export-button.tsx
│   │       │   ├── table-row-dnd.tsx
│   │       │   ├── table-column-dnd.tsx
│   │       │   ├── table-column-actions.tsx
│   │       │   ├── table-column-title.tsx
│   │       │   ├── table-column-sort.tsx
│   │       │   ├── table-column-hide.tsx
│   │       │   ├── table-column-pin.tsx
│   │       │   ├── table-column-faceted-filter.tsx
│   │       │   ├── table-column-slider-filter.tsx
│   │       │   └── table-column-date-filter.tsx
│   │       │
│   │       ├── config/                      # Configuration + feature detection
│   │       │   ├── data-table.ts
│   │       │   └── feature-detection.ts
│   │       │
│   │       ├── hooks/                       # Custom React hooks
│   │       │   ├── use-debounce.ts
│   │       │   ├── use-derived-column-title.ts
│   │       │   ├── use-generated-options.ts
│   │       │   ├── use-keyboard-shortcut.ts
│   │       │   ├── use-data-table-flash.ts                      # flashRows / flashCells helpers
│   │       │   └── use-data-table-scroll.ts                     # scrollRowIntoView helper
│   │       │
│   │       ├── lib/                         # Utility functions
│   │       │   ├── constants.ts
│   │       │   ├── data-table.ts
│   │       │   ├── filter-functions.ts
│   │       │   ├── filter-rows.ts
│   │       │   ├── build-faceted-options.ts
│   │       │   ├── format.ts
│   │       │   ├── styles.ts
│   │       │   ├── column-resize-handle.tsx                     # Shared resize grip (headers)
│   │       │   ├── flex-columns.ts                              # Flex fill + header-fit width resolution (on by default)
│   │       │   ├── use-header-min-widths.ts                     # Header-fit: measure header labels off-DOM to floor widths
│   │       │   ├── use-column-auto-fit.ts                       # Opt-in even-split fill: grow resizable columns to width on load
│   │       │   ├── use-column-sizing-persistence.ts             # Opt-in: persist resized widths to localStorage
│   │       │   ├── create-scroll-handler.ts                     # Shared scroll-listener factory used by every body
│   │       │   └── row-click.ts                                    # Shared row-click guard used by every body
│   │       │
│   │       └── types/                       # TypeScript types
│   │           └── index.ts
│   │
│   └── lib/
│       └── utils.ts                         # cn() utility (already in your project)
│
└── package.json
```

Prefer the [CLI install](/getting-started/installation/) when you only need a subset — each registry item pulls its own dependencies. Manual copy is best when you want the full tree (including optional Data Grid under `grid/`).

## All Components

Browse and copy individual files below (includes `grid/` when present on disk):

<ManualInstallationCode />

## Additional Dependencies

For advanced features, you may need additional dependencies:

### URL State Management (nuqs)

For URL state persistence in tables:

<CliCommandCode action="install" command="nuqs" />

### Virtualization

For virtualized tables (and Data Grid):

<CliCommandCode action="install" command="@tanstack/react-virtual" />

### Sortable Rows / Column DnD

For drag-and-drop row or column reordering, install `@dnd-kit/*` as used by the row-dnd / column-dnd registry items, or follow the [DiceUI Sortable installation guide](https://www.diceui.com/docs/components/sortable).

## Next Steps

Now that you have all the components installed, check out the examples:

- [Simple Table](/examples/simple-table/) - Basic table with no pagination
- [Basic Table](/examples/basic-table/) - Add pagination and sorting
- [Search Table](/examples/search-table/) - Add search functionality
- [Faceted Filter Table](/examples/faceted-filter-table/) - Add advanced filtering
- [Column Resize Table](/examples/column-resize-table/) - Drag / double-click column widths
- [Data Grid](/data-grid/introduction/) - Editable spreadsheet on top of the virtualized table
- [Advanced Table](/examples/advanced-table/) - All features combined
- [Components catalog](/getting-started/components/) - Every `@niko-table/*` registry item
